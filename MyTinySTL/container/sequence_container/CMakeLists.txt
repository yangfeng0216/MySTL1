FILE(GLOB source_file CONFIGURE_DEPENDS ./*.cpp)
FILE(GLOB source_head_file CONFIGURE_DEPENDS ./*.h)
add_library(seq_container ${source_file} ${source_head_file})
target_include_directories(seq_container PUBLIC .)
target_link_libraries(seq_container PRIVATE utils iterator function)